import { View, Text, SafeAreaView } from 'react-native';

import '../global.css';

const RootLayout = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>RootLayout</Text>
      </View>
    </SafeAreaView>
  );
};

export default RootLayout;
